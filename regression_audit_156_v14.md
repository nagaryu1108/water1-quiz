# 156問版 回帰監査 v14

最終更新: 2026-09-04

## 目的
第8弾追加後の156問・780肢について、問題バンク品質、反応式、計算正答、保存、頻度スケジューラ、図表UIの退行を検出する。

## 静的確認済み
- 第8弾 `qbank_gap_batch8_v7.js` は G29-G31 / W29-W31 / T30-T32 / H29-H31 / L29-L31 の15問を追加。
- T32 BOD希釈計算：DO差5.0 mg/L × 20倍 = 100 mg/L、正答index=3（4番肢）。
- T31 MAP：`Mg^2+ + NH4^+ + PO4^3− + 6 H2O → MgNH4PO4·6H2O↓`。
- H29：`CN^− + OCl^− → CNO^− + Cl^−`。
- H30：`Hg^2+ + 2 e^− → Hg^0↑`。
- H31：Cr(VI)還元とCr(OH)3沈殿の2式。
- 化学式描画は `qbank_chem_v7.js` の `window.water1ChemHTML` で `<sup>/<sub>` へ変換する。
- `qbank_frequency_scheduler_v7.js` は、歴史的頻度の確認下限値 × 個人の誤答傾向で重み付けする。
- `index.html` は回答状態を `localStorage` キー `water1_bank_v3` に回答直後保存する実装を持つ。
- 既存図表は G06/W06/T06/H06/L06/T08（問題用）、G04/W05（解説補助）を品質監査対象として維持。

## 自動回帰監査
`qbank_regression_audit_v14.js` を追加し、ブラウザ起動時に以下を自動検査する。
1. QBANK=156
2. 実出題対象Q=156
3. 総選択肢=780
4. 全780肢が `【正しい】/【誤り】` から始まる個別解説を持つ
5. 反応式対象13問
6. 用語ミニ解説を持つ問題数
7. T32=100 mg/L
8. L30≈0.23%
9. 頻度×誤答率スケジューラ
10. `water1ChemHTML` の上付き・下付き変換
11. localStorage読み書き
12. 既存問題用図表6件
13. 解説補助グラフ2件
14. 正解緑/誤答赤CSS
15. index側qbank_patchのキャッシュバージョン

監査結果は `window.WATER1_REGRESSION_AUDIT` に保存し、画面上部の監査欄へ `回帰監査：x/y` と表示する。不合格項目は警告枠に表示する。

## 現時点の既知課題
### index.html のキャッシュクエリ
現行 `index.html` では `qbank_patch_v5.js?v=72` が残っている一方、安定ローダー内部はv91まで進んでいる。Service Workerはscriptを `no-cache` 取得するため通常利用時の影響は軽減されるが、完全なキャッシュ安全性の観点ではindex側も更新すべき。

### index.html の旧監査条件
`if(x.av&&x.v) er.push('問題用図と解説補助図の重複指定');` が残っている。要求仕様は「問題用図と回答後補助図が存在すること自体を禁止」ではなく、「同じ内容の冗長表示を避ける」であるため、将来的にはこの条件を削除または意味ベースの重複判定へ変更すべき。

### ランタイム最終確認
GitHub上の静的ソース確認とブラウザ内自己監査コードは実装済みだが、この監査記録作成時点では外部実行環境からGitHub PagesのJavaScript実行結果を直接取得できていない。したがって `156/156・780/780` のランタイム合格は、画面の `回帰監査` 表示で最終確認する。

## 判定
- 問題内容・解説・計算・反応式・保存コード・スケジューラ・図表参照：静的監査上は整合。
- ブラウザ実行の最終合格：自己監査結果待ち。
- indexのキャッシュ版と旧 `av&&v` 条件：技術的負債として残存。
